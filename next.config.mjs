/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['saul-mirage.scaleupdevagency.com','saul-mirage.scaleupdevagency.comuploads','localhost','res.cloudinary.com','loremflickr.com'], // replace with actual domain
  },
};

export default nextConfig;
