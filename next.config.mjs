/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['saul-mirage.scaleupdevagency.com','saul-mirage.scaleupdevagency.comuploads'], // replace with actual domain
  },
};

export default nextConfig;
